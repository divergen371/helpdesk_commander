version: '3.8'

services:
  postgres:
    build:
      context: ./docker/postgres
      dockerfile: Dockerfile
    container_name: helpdesk_commander_postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-postgres}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data_18:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - helpdesk_network

  # Elixirアプリケーション（オプション）
  # 後でDockerでアプリも動かしたい場合に使用
  # app:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: helpdesk_commander_app
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   environment:
  #     DATABASE_URL: postgres://postgres:postgres@postgres:5432/helpdesk_commander_dev
  #   ports:
  #     - "4000:4000"
  #   volumes:
  #     - .:/app
  #   networks:
  #     - helpdesk_network
  #   command: mix phx.server

volumes:
  postgres_data_18:

networks:
  helpdesk_network:
    driver: bridge
