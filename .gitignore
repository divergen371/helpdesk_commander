# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where 3rd-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Temporary files, for example, from tests.
/tmp/

# Ignore package tarball (built via "mix hex.build").
helpdesk_commander-*.tar

# Ignore assets that are produced by build tools.
/priv/static/assets/

# Ignore digested assets cache.
/priv/static/cache_manifest.json

# phx.digest also generates compressed (.gz) and digested copies of static files.
# Keep the *source* static files committed (e.g. priv/static/robots.txt), but ignore generated variants.
/priv/static/**/*.gz
/priv/static/*-*.ico
/priv/static/*-*.txt
/priv/static/images/*-*.svg

# In case you use Node.js/npm, you want to ignore these.
npm-debug.log
/assets/node_modules/

# Dialyzer PLT files
/priv/plts/
*.plt
*.plt.hash

# Environment variables (secrets)
.env
.env.local
.env.*.local

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~
.specstory/
.DS_Store

# Elixir LS (Language Server)
/.elixir_ls/

# Ignore local configuration override
/config/*.secret.exs

# Ignore database files (if using SQLite locally)
*.db
*.db-shm
*.db-wal

# Ignore log files
*.log
/log/

# Ignore uploaded files (if any)
/priv/static/uploads/

# Docker volumes and local data
/postgres_data/

# Backup files
*.bak
*.backup

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

